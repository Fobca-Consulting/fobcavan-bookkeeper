// This file is simply re-exporting the actual hook from another location
// We're keeping this structure to maintain compatibility with existing imports

import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
